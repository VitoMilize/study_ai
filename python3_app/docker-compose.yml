version: "3.9"

services:
  app:
    build: .
    container_name: hospital_app
    ports:
      - "8888:8888"
    environment:
      REDIS_HOST: redis
      REDIS_PORT: 6379
    depends_on:
      - redis

  redis:
    image: redis:7
    container_name: redis
    ports:
      - "6379:6379"
